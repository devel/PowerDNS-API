package PowerDNS::API::App;
use strict;
use warnings;

# /static/js/ - static js files
# /static/css/ - static css files
# /static/templates/ - fetch "compiled" mustache templates

# /api/login
#   (get token)

# domains
# /api/list_domains (by type, name, ...)
#     /add_domain (slave, master, ...)
#     /list_records (by domain (required), name, type
#     /add_record
#     /delete_record
#     /update_record

# internal methods
#   increment serial (automatically)




1;
